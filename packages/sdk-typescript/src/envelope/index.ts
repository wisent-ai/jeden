// What an envelope is: the shapes it takes, the field names generated from
// the canonical schema, and the validation that refuses anything else.
//
// Grouped here so the SDK source folder keeps to five entries; the package
// index re-exports these under the names consumers already import.

export * from "./types.js";
export * from "./validators.js";
