# Written by the wisent-visuals banner bot. The banner below is rendered
# from .github/banner.toml and replaced whole on every run, so a repository
# audit reads it as generated output instead of asking somebody to split it.
assets/readme-banner.svg

# The repository root. Every file left in it is fixed there by a tool that
# reads it by that exact path: cargo reads Cargo.toml and Cargo.lock and runs
# build.rs, npm reads package.json and package-lock.json, git reads
# .gitignore, dotenv reads .env.example, the Wisent release tooling reads
# .wisent-release.json and platform-entitlements.json, and the published
# surface check reads released-surface.json. LICENSE, NOTICE and README.md are
# read by people and by every registry this package is published to. Moving
# any of them into a sub-folder breaks the tool that names the path.
.

# The published site's own root. A browser and the host read each of these by
# the exact path it is served from: /favicon.svg, /robots.txt, /og-image.png
# and /og-image.svg from the page metadata, /styles.css and /script.js from
# index.html, and vercel.json by the platform that deploys it. Moving one into
# a sub-folder changes the URL and breaks the reader that names it.
web/favicon.svg
web/index.html
web/og-image.png
web/og-image.svg
web/robots.txt
web/script.js
web/styles.css
web/vercel.json

# The machine-written translation overlays. Both open with "GENERATED by
# i18n-gen — regenerate, do not hand-edit", so splitting either by hand is
# exactly what their own banner forbids, and a split would be undone the next
# time they are produced. They carry no logic: each is a table of
# (language code, key, text) rows that `i18n::tr` and the prompt assembly read
# after their hand-written rows. Defect b5d61f80 records that the named
# generator is not in this repository, which is the part that still has to be
# repaired; this declaration keeps the audit from ordering a change the banner
# refuses.
rust/cli/i18n/translations.rs
rust/agent/runtime/contracts/language_prose.rs
