const selection = "Optional <code>--session &lt;id-or-path&gt;</code> selects a recorded session; otherwise the workspace's latest session is used. <code>--json</code> returns the authoritative completion snapshot.";
const controls = "An original request ID controls every unfinished task it owns, including an unplanned request when model intake is unavailable. A task ID controls that task only. <code>--revision</code> must match the latest snapshot and <code>--reason</code> must explain the operator's action.";
const failures = [
  "Without a selected or current session: <code>no active session; use --session &lt;id-or-path&gt;</code>.",
  "Missing control fields: <code>task control requires --revision</code> or <code>task control requires --reason</code>; an empty explanation is refused as <code>task control requires a nonempty operator reason</code>.",
  "A stale revision is refused as <code>completion state changed: expected revision &lt;expected&gt;, found &lt;actual&gt;</code>; refresh the snapshot before choosing another action.",
  "Unknown IDs are refused as <code>unknown task or request: &lt;id&gt;</code>. Completed or cancelled work is terminal and cannot be resumed by changing its status.",
];

export const completionCommands = [
  {
    path: "todo",
    invocation: 'jeden todo [list|add "request"|pause <id>|resume <id>|cancel <id>|defect <id>|answer <id> --text "answer"|continue] [--session <id-or-path>] [--revision <n> --reason "text"] [--json]',
    purpose: "Inspect and control every retained user request and its verified task state.",
    inputs: [selection, controls],
    effect: "Stores requests, acceptance criteria, task states, independent observations, actual failures and what a blocked task asks of you in the session's atomic <code>completion.json</code>. The agent's todo done action requests verification; it cannot mark work complete. Starting another question, clearing model context, or resuming a recorded session does not discard retained obligations. Legacy task claims are imported for verification rather than trusted as successful.",
    refusals: [...failures, "There is no operator <code>done</code> command: <code>Task completion requires independent verification; there is no operator done command.</code>"],
  },
  {
    path: "todo/list",
    invocation: "jeden todo list [--session <id-or-path>] [--json]",
    purpose: "Read retained requests, tasks, acceptance requirements and completion evidence.",
    inputs: [selection],
    effect: "Prints status, revision, original requests, acceptance criteria, failed operations and verified session/event references. A task that waits on you shows <code>Waiting on you: &lt;ask&gt;</code> and the exact <code>jeden todo answer</code> command, and the status reads <code>waiting_for_operator</code> until you answer; an answered ask stays beside the task as <code>Asked of you</code> and <code>Your answer</code>. <code>complete: false</code> remains explicit when there is unplanned, pending, paused, interrupted, blocked, waiting or unverified work. Reading a legacy workspace list migrates that superseded list before displaying its session-owned replacement.",
    refusals: [failures[0], "Invalid or unreadable completion files return their storage or schema error, never an empty successful task list."],
  },
  {
    path: "todo/add",
    invocation: 'jeden todo add "request" [--session <id-or-path>] [--json]',
    purpose: "Record a user request without executing it or inventing its acceptance result.",
    inputs: [selection, "Required: nonempty request text. When no active session exists, a new durable session is created."],
    effect: "Appends an unplanned request and preserves every earlier request. It does not call a model. Desktop and mobile expose Add without running in their Tasks panels. Continue later to record acceptance criteria independently and execute the request.",
    refusals: ["Missing text returns the todo usage. Storage failures are reported without claiming the request was completed."],
  },
  ...[
    ["pause", "Pause retained work without losing it.", "Marks the selected unfinished work paused. It remains incomplete and the execution agent cannot resume it itself."],
    ["resume", "Make paused retained work eligible to run again.", "Returns the selected work to pending; it does not execute it. Use todo continue or Continue retained work in the graphical session to run it with the current grants."],
    ["cancel", "Withdraw exactly the work the operator selected.", "Records the operator's reason and cancellation without deleting the original request, completed evidence or other work. A cancelled request is not reported as verified implementation."],
  ].map(([action, purpose, effect]) => ({
    path: `todo/${action}`,
    invocation: `jeden todo ${action} <task-or-request-id> --revision <n> --reason "text" [--session <id-or-path>] [--json]`,
    purpose,
    inputs: [selection, controls],
    effect,
    refusals: failures,
  })),
  {
    path: "todo/defect",
    invocation: 'jeden todo defect <task-or-request-id> --revision <n> --reason "what failed" [--session <id-or-path>] [--json]',
    purpose: "Record a defect and reopen its original task for repair.",
    inputs: [selection, controls, "The selected original task may already be complete. An explicit defect report in a conversation is also linked to its original task during independent intake."],
    effect: "Atomically records a task with kind defect and defectOf pointing to the original task or request. The original acceptance criteria remain, its earlier verification is cleared, and both the original result and the repair require fresh independent observations. A defect in an earlier repair points back to the same original obligation. It does not execute the repair; use todo continue. Desktop and mobile Tasks panels expose Report defect and display the original target.",
    refusals: [failures[0], failures[1], failures[2],
      "Unknown targets are refused as <code>unknown task or request: &lt;id&gt;</code>.",
      "Cancelled work cannot be reopened: <code>cancelled task cannot be reopened by a defect: &lt;id&gt;</code> or <code>cancelled request cannot be reopened by a defect: &lt;id&gt;</code>.",
      "A duplicate open report is refused as <code>the same defect is already open for &lt;id&gt;</code>. A rejected operation leaves the saved state unchanged.",
      "An operator pause stays in force. Recording a defect does not authorize resuming paused work.",
    ],
  },
  {
    path: "todo/answer",
    invocation: 'jeden todo answer <task-id> --revision <n> --text "answer" [--session <id-or-path>] [--json]',
    purpose: "Give a blocked task the one thing it asked you for.",
    inputs: [selection, "<code>--revision</code> must match the latest snapshot. <code>--text</code> is your answer: the value, the decision, or where you put the secret. Over RPC the same operation is <code>session/completion/control</code> with action <code>answer</code> and the answer in <code>reason</code>; Jeden Desktop shows a Your answer field under the task."],
    effect: "A task blocks with an ask when the independent review finds that only you can supply what it needs: a secret, a credential, a decision, a value that exists in no file. The ask is recorded on the task in one sentence, shown by todo list, in the stop message and in the graphical Tasks panel, and the session stops as <code>waiting_for_operator</code> instead of retrying. Your answer is recorded beside the ask with its time, the task returns to pending, and the next todo continue hands the agent your words as yours; the review refuses to block that task again unless an operation failed after your answer. The agent has no way to answer its own ask.",
    refusals: [failures[0], failures[2],
      "Missing fields: <code>answer requires --text with what the task asked for</code> or <code>answer requires --revision</code>; blank text is refused as <code>an answer requires nonempty text</code>.",
      "A task that asked nothing is refused as <code>task &lt;id&gt; asks nothing of the operator; resume it with a reason instead</code>; a request ID is refused as <code>unknown task: &lt;id&gt;</code>.",
      "A second answer is refused as <code>task &lt;id&gt; already holds an answer to its request</code>; completed or cancelled work as <code>task is already terminal: &lt;id&gt;</code>.",
    ],
  },
  {
    path: "todo/continue",
    invocation: "jeden todo continue [--session <id-or-path>] [--allow-write] [--allow-command] [--model name] [--max-steps n] [--json]",
    purpose: "Inspect previous results and continue retained work without adding a synthetic user request.",
    inputs: [selection, "The recorded session workspace is used. Write and command permissions are explicit for this invocation; continuation never expands them."],
    effect: "Opens the retained session, records any missing acceptance criteria and independently inspects the existing results before execution. A proposed final answer is withheld while any actionable request remains unfinished. Transport failure, operator interruption or an execution limit leaves an incomplete snapshot with its actual cause. Complete work returns without another model call.",
    refusals: ["Paused work is refused as <code>Retained work is paused; resume its tasks before continuing.</code>.", "A task waiting on you is not worked around: the run stops again as <code>Work remains waiting_for_operator</code> with the ask and its answer command until you answer.", "Unavailable Brama access or independent review is a failed run with retained work, not completion. <code>execution_limit</code> means paused and <code>turn_cancelled</code> means interrupted; neither status means done."],
  },
];
